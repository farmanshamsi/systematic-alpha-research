[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "systematic-alpha-research"
version = "0.1.0"
description = "Quantitative alpha research platform for trend, cointegration, Ornstein-Uhlenbeck dynamics, event-driven backtesting, portfolio diversification, and paper execution research"
readme = "README.md"
requires-python = ">=3.11,<3.13"

dependencies = [
    "alpaca-py>=0.43.5",
    "arch",
    "cvxpy",
    "matplotlib",
    "numpy",
    "openpyxl",
    "pandas",
    "pyarrow",
    "PyYAML",
    "python-decouple",
    "scikit-learn",
    "scipy",
    "statsmodels",
    "yfinance",
    "exchange-calendars==4.13.2",
]

[project.optional-dependencies]
dev = [
    "pytest>=8",
    "pytest-cov",
    "wheel",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-q"
